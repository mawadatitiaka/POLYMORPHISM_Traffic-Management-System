# POLYMORPHISM_Traffic-Management-System
This repo contains codes that emphasize on basic polymorphism
Each of the child classes that inherit from the parent class TrafficDevice all make use of the activate() method.
But they all use it differently because they are different classes
